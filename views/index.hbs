<h1>{{appTitle}} 🚀</h1>



<div>
{{#if user}}
   <p>Welcome <a href="/user/{{user._id}}">{{user.username}}</a>!</p>
{{else}}
    <p>Welcome!</p>
{{/if}}
</div>

<div>
{{#each result}}
    <div>
        <img src="{{image}}" alt="">
        {{title}}
    </div>
{{/each}}
</div>